.net 10 upgrade (w/ansible)
playoffs endpoint + dtos
controllers-permissions table
automated integration tests
unit tests
proper ansible separation of concerns and abstraction (server info file from terraform)
JWT -- why?
svelte frontend
fix playoffs
schedule upload
stats system